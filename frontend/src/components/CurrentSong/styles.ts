import styled from 'styled-components';

export const Wrapper = styled.div`
  overflow: hidden;
  width: 100%;
  height: 6rem;
  background: #1f2232;
  position: fixed;
  bottom: 0;
  left: 0;
  z-index: 100;
`;
